[build]
builder = "dockerfile"

[deploy]
startCommand = "node dist/index.cjs"
healthcheckPath = "/api/roster/york-prison"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[deploy.volumes]]
mountPath = "/data"

